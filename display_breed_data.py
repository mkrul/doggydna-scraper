import cv2
import os
import sys
import pdb as p
import json



# for each in results:
#   for filename in results[each]["breeds"]["src"]:
#     img = cv2.imread(filename, cv2.IMREAD_ANYCOLOR)
#     cv2.imshow(filename, img)
#     k = cv2.waitKey(0) & 0xFF
#     if k == 27:
#       cv2.destroyAllWindows()
#       continue
